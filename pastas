cd /
sudo mkdir tablespc
cd tablespc
sudo mkdir tbspc01
sudo mkdir tbspc02
cd /
sudo chown postgres.postgres /tablespc
sudo chown postgres.postgres /tablespc/tbspc01
sudo chown postgres.postgres /tablespc/tbspc02
